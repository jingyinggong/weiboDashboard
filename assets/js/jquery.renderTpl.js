// Generated by CoffeeScript 1.3.3

(function() {
  return $.fn.renderTpl = function(opts) {
    var clear, data, el, fn, method, tpl;
    el = $(this);
    data = opts['data'] || [];
    tpl = opts['tpl'] || '';
    method = opts['method'] || 'append';
    clear = opts['clear'] || true;
    fn = opts['callback'];
    clear && el.empty();
    _.each(data, function(v, k) {
      var str;
      str = _.template(tpl, v);
      return el[method](str);
    });
    if (_.isFunction(fn)) {
      return fn.call(this);
    }
  };
})(jQuery);
